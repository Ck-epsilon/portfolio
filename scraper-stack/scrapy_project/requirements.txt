# Core — production-grade web scraping framework
# security: Scrapy >= 2.13.3 (fixes CVE-2025-6176 brotli decompression bomb)
Scrapy>=2.13.3
brotli>=1.2.0

# JavaScript rendering — real Chromium via Playwright
scrapy-playwright>=0.0.40
playwright>=1.52.0

# Data validation
pydantic>=2.0

# Export
openpyxl>=3.1.0

# HTTP client (Scrapy uses Twisted, but we need async HTTP for health checks)
httpx>=0.27.0

# YAML config parsing
PyYAML>=6.0
